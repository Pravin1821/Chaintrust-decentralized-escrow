# ========================================
# CHAINTRUST BACKEND CONFIGURATION
# ========================================
# CRITICAL: Copy this file to .env and fill in all values
# NEVER commit .env to version control

# ========================================
# SECURITY (REQUIRED - NO DEFAULTS)
# ========================================
# JWT Secret - Use strong random string (min 32 chars)
# Generate: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-min-32-characters-long

# ========================================
# DATABASE (REQUIRED)
# ========================================
MONGO_URI=mongodb://localhost:27017/chaintrust
# OR MongoDB Atlas:
# MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/chaintrust?retryWrites=true&w=majority

# ========================================
# SERVER CONFIGURATION
# ========================================
PORT=5000
NODE_ENV=development

# ========================================
# CORS CONFIGURATION
# ========================================
# Frontend URL - Update for production
FRONTEND_URL=http://localhost:5173

# ========================================
# PRODUCTION CHECKLIST
# ========================================
# Before deploying to production:
# 1. Generate strong JWT_SECRET (min 32 chars)
# 2. Set NODE_ENV=production
# 3. Update FRONTEND_URL to your production domain
# 4. Use MongoDB Atlas or secure MongoDB instance
# 5. Never expose this .env file publicly
# 6. Enable HTTPS/SSL certificates
# 7. Review rate limiting settings in server.js
